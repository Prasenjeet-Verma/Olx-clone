<%- include('partials/head') %>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <!-- Full width header -->
  <%- include('partials/header') %>

  <!-- Centered content container -->
  <div class="flex flex-col items-center w-full bg-white pt-6">

    <!-- Profile Info -->
    <div class="w-full max-w-md flex flex-col items-center">

      <!-- Profile Picture -->
      <img
        src="<%= currentUser.profilePic ? currentUser.profilePic : '/uploads/default.png' %>"
        alt="Profile"
        class="w-20 h-20 rounded-full shadow"
      />

      <!-- Username -->
      <h2 class="text-lg font-semibold text-gray-900"><%= currentUser.username %></h2>

      <!-- Member since -->
      <div class="text-sm text-gray-500 flex items-center gap-1 mt-1">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-gray-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3M16 7V3M4 11h16M4 19h16M4 15h16"/>
        </svg>
        Member since 
        <span class="ml-1">
          <%= new Date(currentUser.createdAt).toLocaleDateString("en-IN", {
            month: "short",
            year: "numeric"
          }) %>
        </span>
      </div>

      <!-- Student tag -->
      <div class="text-sm text-gray-700 mt-1">🎓 Student</div>

      <!-- Verification Icons -->
      <div class="flex items-center gap-2 mt-2">
        <!-- Replace with real verification logic -->
        <span title="Phone verified">📞</span>
        <span title="Email verified">📧</span>
        <span title="Govt ID verified">🆔</span>
      </div>

      <!-- Buttons -->
      <div class="w-full mt-4 px-4">
        <a href="/edituserprofile" class="block w-full text-center py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition">
          ✏️ Edit profile
        </a>
      </div>

      <a href="/userprofile" class="text-blue-600 text-sm mt-2 hover:underline">Share Profile</a>
    </div>

    <!-- No Listings -->
    <div class="w-full max-w-md text-center mt-8 px-4">
      <img src="/images/empty-state-illustration.png" alt="No Listings" class="w-32 mx-auto mb-4" />
      <p class="text-gray-700 font-medium">You haven't listed anything yet</p>
      <p class="text-sm text-gray-500">Let go of what you don't use</p>
      <a href="/sell" class="inline-block mt-4 px-5 py-2 bg-blue-600 text-white text-sm rounded-md hover:bg-blue-700 transition">
        Start Selling
      </a>
      <!-- Footer Section -->
<div class="w-full max-w-md mt-10 text-center">

  <!-- Follow Us -->
  <p class="text-gray-600 text-sm font-semibold mb-2">FOLLOW US</p>
  <div class="flex justify-center gap-4 text-blue-600 text-xl mb-6">
    <!-- You can replace these emojis/icons with Font Awesome or actual SVGs -->
    <a href="#"><i class="fab fa-facebook-f"></i></a>
   <a href="#" aria-label="Instagram" class="hover:text-pink-600">
    <i class="fab fa-instagram"></i>
  </a>

  <a href="#" aria-label="YouTube" class="hover:text-red-600">
    <i class="fab fa-youtube"></i>
  </a>

  <a href="#" aria-label="X (Twitter)" class="hover:text-black">
    <i class="fab fa-x-twitter"></i> <!-- For Font Awesome 6+ -->
    <!-- If you're using FA 5, use: <i class="fab fa-twitter"></i> -->
  </a>

  <a href="#" aria-label="WhatsApp" class="hover:text-green-600">
    <i class="fab fa-whatsapp"></i>
  </a>

  <a href="#" aria-label="LinkedIn" class="hover:text-blue-500">
    <i class="fab fa-linkedin-in"></i>
  </a>
  </div>

  <!-- Group Logos -->
  <div class="bg-blue-900 text-white py-6 px-4 rounded-t-lg">
    <p class="font-semibold text-sm mb-4">CarTrade <span class="text-white bg-blue-700 px-2 py-1 rounded">Tech</span> GROUP</p>

    <div class="grid grid-cols-3 gap-4 text-xs font-medium mb-6">
      <div>OLX</div>
      <div>CarWale</div>
      <div>BikeWale</div>
      <div>CarTrade</div>
      <div class="col-span-2">Mobility Outlook</div>
    </div>

    <p class="text-xs text-white/70">All rights reserved © 2006–2025 OLX</p>
  </div>
</div>

    </div>

  </div>
</body>
</html>

